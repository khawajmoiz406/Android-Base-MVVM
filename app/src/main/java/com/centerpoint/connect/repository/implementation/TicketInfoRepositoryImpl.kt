package com.centerpoint.connect.repository.implementation

import com.centerpoint.connect.api.Apis
import com.centerpoint.connect.repository.TicketInfoRepository

@Suppress("UNCHECKED_CAST")
class TicketInfoRepositoryImpl(private val apis: Apis) : TicketInfoRepository